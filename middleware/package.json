{
  "name": "mina-zec-barter-middleware",
  "version": "0.1.0",
  "description": "Stateless middleware coordinator for MINA ↔ ZEC atomic swaps",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "reset:local": "rm -rf .state ../zcash/escrowdv2/data ../mina/escrowm-init/src/scripts/.state",
    "dev": "tsc && node build/index.js",
    "watch": "tsc --watch",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:unit": "NODE_OPTIONS=--experimental-vm-modules jest __tests__/unit",
    "test:integration": "NODE_OPTIONS=--experimental-vm-modules jest --config jest.integration.config.js",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "test:integration:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --config jest.integration.config.js --coverage"
  },
  "dependencies": {
    "o1js": "^2.0.0",
    "dotenv": "^16.4.5",
    "axios": "^1.6.7",
    "@supabase/supabase-js": "^2.45.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "@types/express": "^4.17.21",
    "typescript": "^5.4.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "@types/jest": "^29.5.14"
  },
  "engines": {
    "node": ">=18.14.0"
  }
}
